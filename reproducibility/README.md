# Reproducibility environment snapshot

`current_author_sessionInfo.txt` and `current_author_package_versions.csv` capture the author's current local R environment at repository-cleanup time. They are provided for transparency and dependency guidance.

They may differ from the original analysis environment reported in the manuscript. The manuscript remains the authoritative study record. Refresh the local snapshot with:

```text
Rscript reproducibility/capture_session_info.R
```
